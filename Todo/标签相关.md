请修改我的标签结构，目前是统一字段：单曲：《花火》，专辑：《鲍家街43号》这样，我希望分成两节来管理，前面是种类，后面是值，比如单曲和专辑是种类，后面的是值，但是最终显示是拼接在一起，种类后续可以扩展，比如：艺人、演唱会、音乐节、直播等等等等。请先修改数据库里的字段，数据库表结构，然后修改我的标签管理页面，在标签管理页面新增一个 创建标签 功能，先选择种类，再输入值。种类也支持创建，所以就不需要种类的图标了。将标签以列表的形式展示。标签管理页面支持删除和修改标签。



请把文章发布的步骤从两步改为三步，

第二步的页面右上角的按钮改为上一步和下一步

将第二步的选择标签相关的功能移到第三步

在第三步的页面，我希望能根据正文内容自动匹配一些标签，这个功能可以实现吗？请评估一下，如果很复杂的话就不要了，放进后期优化中。建议采用关键字匹配。然后是一个搜索框，可以通过关键字搜索标签，然后选择添加。并且新增一个功能，创建标签，和标签管理页面的创建标签一样，并且数据是互通的。后续能在标签管理页面进行修改和删除之类的操作。