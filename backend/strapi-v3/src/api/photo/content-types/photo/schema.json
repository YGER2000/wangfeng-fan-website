export default {
  kind: 'collectionType',
  collectionName: 'photos',
  displayName: 'Photo',
  singularName: 'photo',
  pluralName: 'photos',
  description: '照片',
  attributes: {
    // 图片
    image: {
      type: 'media',
      allowedTypes: ['images'],
      multiple: false,
      required: true,
    },

    // 描述
    description: {
      type: 'text',
      maxLength: 500,
    },

    // 排序
    sortOrder: {
      type: 'integer',
      default: 0,
    },

    // 所属图组
    photoGroup: {
      type: 'relation',
      relation: 'manyToOne',
      target: 'api::photo-group.photo-group',
      inversedBy: 'photos',
      required: true,
    },
  },
};
